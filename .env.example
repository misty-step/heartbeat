# =============================================================================
# Heartbeat Environment Variables
# =============================================================================
# Copy this file to .env.local and fill in your values.
# Never commit .env.local to version control.

# =============================================================================
# REQUIRED - App won't start without these
# =============================================================================

# Convex - Get from convex.dev dashboard after running `bunx convex dev`
CONVEX_DEPLOYMENT=dev:your-deployment-name
NEXT_PUBLIC_CONVEX_URL=https://your-deployment.convex.cloud

# Clerk - Get from dashboard.clerk.com > API Keys
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_xxx
CLERK_SECRET_KEY=sk_test_xxx
# JWT issuer domain - Get from JWT Templates > Default > Issuer
CLERK_JWT_ISSUER_DOMAIN=https://your-domain.clerk.accounts.dev

# =============================================================================
# RECOMMENDED - Core features need these
# =============================================================================

# Stripe - Get from dashboard.stripe.com/apikeys
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_xxx
STRIPE_SECRET_KEY=sk_test_xxx
STRIPE_WEBHOOK_SECRET=whsec_xxx

# Stripe Price IDs - Create products in Stripe, then get price IDs
STRIPE_PRICE_PULSE_MONTHLY=price_xxx
STRIPE_PRICE_PULSE_YEARLY=price_xxx
STRIPE_PRICE_VITAL_MONTHLY=price_xxx
STRIPE_PRICE_VITAL_YEARLY=price_xxx

# =============================================================================
# OBSERVABILITY - Production monitoring
# =============================================================================

# Sentry - Get from sentry.io project settings
# Note: Both DSNs should have the same value
NEXT_PUBLIC_SENTRY_DSN=https://xxx@xxx.ingest.us.sentry.io/xxx
SENTRY_DSN=https://xxx@xxx.ingest.us.sentry.io/xxx
SENTRY_AUTH_TOKEN=sntrys_xxx
SENTRY_ORG=your-org
SENTRY_PROJECT=heartbeat

# PostHog - Get from us.posthog.com project settings
NEXT_PUBLIC_POSTHOG_KEY=phc_xxx
# Use /ingest in production (bypasses ad blockers via Next.js rewrite)
# Use https://us.i.posthog.com in local dev (avoids 431 cookie size errors from Clerk JWT tokens)
NEXT_PUBLIC_POSTHOG_HOST=/ingest

# =============================================================================
# OPTIONAL - Enhanced features
# =============================================================================

# Resend - For email notifications (get from resend.com/api-keys)
RESEND_API_KEY=re_xxx

# App URL - Set in production for absolute URLs
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Logging - Set to 'debug' for verbose logs
LOG_LEVEL=info
