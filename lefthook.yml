pre-commit:
  parallel: true
  commands:
    lint:
      glob: "*.{ts,tsx}"
      exclude: "_generated"
      run: bun eslint {staged_files} --max-warnings 0 --no-warn-ignored
    format:
      glob: "*.{ts,tsx,json,md,css}"
      run: bun prettier --write {staged_files}
      stage_fixed: true
    typecheck:
      run: bun tsc --noEmit

pre-push:
  commands:
    test:
      run: bun test

commit-msg:
  commands:
    lint-commit:
      run: bun commitlint --edit {1}
